# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET        /                                controllers.Application.index
GET        /test                            controllers.Application.test
GET        /clusters                        controllers.Clusters.index
#GET        /clusters                        controllers.Clusters.find(page: Int ?= 1)
#GET        /clusters/$id<[0-9]+>            controllers.Clusters.findOne(id: Int)
#GET        /clusters/redirect               controllers.Clusters.redirect
#POST       /clusters/$id<[0-9]+>            controllers.Clusters.save()
GET       /clusters/add                    controllers.Clusters.add
#GET        /servers                        controllers.Clusters.find(page: Int ?= 1)
#GET        /servers/$id<[0-9]+>            controllers.Clusters.findOne(id: Int)
#POST       /servers/$id<[0-9]+>            controllers.Clusters.save()
#POST       /servers/add                    controllers.Clusters.save()
#GET        /tenants                        controllers.Clusters.find(page: Int ?= 1)
#GET        /tenants/$id<[0-9]+>            controllers.Clusters.findOne(id: Int)
#POST       /tenants/$id<[0-9]+>            controllers.Clusters.save()
#POST       /tenants/add                    controllers.Clusters.save()




# Map static resources from the /public folder to the /assets URL path
GET        /assets/*file        controllers.Assets.versioned(path="/public", file: Asset)



